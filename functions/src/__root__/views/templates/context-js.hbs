(function(window, ctx) {
  site.context = {
    firePageData: {{stringify firePageData}},
    products: {{stringify products}},
    product: {{stringify product}},
    activeVariant: {{stringify activeVariant}},
    pageSettings: {{stringify pageSettings}},
    fireBaseData: {{stringify fireBaseData}},
    strings: {{stringify strings}},
    siteSettings: {{stringify siteSettings}},
    fireUser: {{stringify fireUser}},
    settings: {{stringify settings}}
  };
})(window, window.site);
